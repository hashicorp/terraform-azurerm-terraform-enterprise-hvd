# Tasks: Readiness API and admin console support

1. Add readiness path local logic and admin-console variables in `variables.tf` and `compute.tf`.
2. Update `load_balancer.tf`, `outputs.tf`, and `templates/tfe_custom_data.sh.tpl`.
3. Update `examples/main/*`, `README.md`, and docs.
4. Run `task test` and `task terraform-docs`.

